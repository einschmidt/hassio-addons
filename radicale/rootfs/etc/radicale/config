[server]
# Bind all addresses
hosts = 0.0.0.0:5232, [::]:5232

[auth]
type = htpasswd
htpasswd_filename = /share/radicale/users
htpasswd_encryption = md5

[storage]
filesystem_folder = /data/radicale

[headers]
Access-Control-Allow-Origin = *

[logging]
level = info